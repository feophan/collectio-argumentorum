---
const { tags } = Astro.props;

interface Props {
    tags: string[];
}
---

<div class="flex flex-wrap gap-2">
    {tags.map((tag: string) => (
        <span class="px-2 py-1 bg-blue-500 text-white rounded-full text-xs hover:opacity-90">{tag}</span>
    ))}
</div>